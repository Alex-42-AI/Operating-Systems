Write a C program, that implements the cat command (without parameters).
Write a C program, that implements the tee file command.
Write a C program, that implements the cat >> file command.
Write a C program, that implements the cat [file] [...] command.
Write a C program, that implements the cut -cm-m file command (m and n are read from the input).
(*) Write a C program, that implements the fgrep -q text file command (assume the lines are up to 80 symbols).
