Run the LOGINFO procedure in your profile
Write a command procedure, that receives 2 numbers passed as parameters at the command line. It subtracts the lesser one from the greater one and prints out the result.
Write a command procedure, that receives 2 numbers passed as parameters at the command line. It prints out a sequence of all integers between them.
(NAME_CUT) Write a command procedure, that receives the name of an existing directory as a parameter at the command line. For each file in it, that contains C code, the procedure starts compilation in background mode, setting the main name of the text file with a .exe extention as a name of the executable file, that appears as a result of a successful compilation.
